/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // comment this line if running npm run start | uncomment if running npm run deploy
    output: 'standalone'
}

module.exports = nextConfig
